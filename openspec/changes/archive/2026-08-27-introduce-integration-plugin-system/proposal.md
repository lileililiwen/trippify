# Why
The ecosystem needs constrained extensibility.

# What Changes
Add versioned manifests, scoped permissions, signed installation, lifecycle, catalog, audit, and provider adapters.

# Capabilities
## New Capabilities
- `integration-plugin-system`: versioned manifests, scoped permissions, signed installation, lifecycle, catalog, audit, and provider adapters.

# Dependencies and Non-goals
- Dependencies: package-self-hosted-edition.
- Non-goals: capabilities assigned to later roadmap changes.

# Impact
Adds the ASP.NET Core API/persistence and Flutter experience for this vertical slice.
